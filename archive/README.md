# Archive

Everything under `archive/` is historical and non-canonical.

The only official midterm-compliant entrypoints live outside this directory:

- `scripts/train_raw_baseline.py`
- `notebooks/kaggle_submit_raw_baseline.ipynb`
- `scripts/audit_midterm_compliance.py`

Archived notebooks are preserved for chronology, ablations, and debugging history only. They should not be treated as the official reproduction or submission path.
